package com.github.searchbadger.util;

import android.test.InstrumentationTestCase;

public class FacebookHelperTest extends InstrumentationTestCase {

	@Override
	protected void setUp() throws Exception {
	}
	
	@Override
	protected void tearDown() throws Exception {
	}
	

	public void testMessageSource() {
		
		/*
		MessageSource source = MessageSource.STARRED;
		
		// test parcelable		
		assertEquals(0, source.describeContents());
		Parcel p = Parcel.obtain();
        p.writeParcelable(source, 0);
        p.setDataPosition(0);
        MessageSource source2 = p.readParcelable(Contact.class.getClassLoader());
        assertEquals(source, source2);
        p.recycle();
        
        MessageSource[] sources = MessageSource.CREATOR.newArray(5);
        assertEquals(5, sources.length);
        */
	}
	
}
